# 快乐风向 AI 开发工作流 V1.0

本文件是本项目永久开发规范，适用于整个项目目录及其所有子目录。

以后本项目所有开发、修改、Bug 修复，都必须严格遵循以下工作流，不允许跳过任何阶段。

## 第一阶段：开发

- 根据需求完成开发。
- 保持代码结构清晰。
- 不允许为了赶进度删除已有功能。
- 每次修改尽量只影响当前需求。

## 第二阶段：自测

每次开发完成后，必须自动完成以下检查：

1. 所有按钮是否可点击。
2. 所有页面是否可以正常跳转。
3. 返回按钮是否正常。
4. 表单是否可以提交。
5. 数据保存是否成功。
6. 后台修改是否同步到前台。
7. 手机端和 PC 端都测试。
8. Console 不能有 Error。
9. Network 不能有 500、404 等错误。
10. 不允许存在死按钮、空白页、无响应按钮。

## 第三阶段：自动修复

如果发现任何 Bug：

- 必须先定位原因。
- 修复 Bug。
- 修复后重新完整测试。
- 循环执行，直到所有 Bug 全部消失。
- 不允许只修复当前 Bug 而导致其他功能失效。

## 第四阶段：交付

只有全部测试通过以后，才能告诉用户项目完成。

交付时必须提供：

1. 修改了哪些文件。
2. 修复了哪些 Bug。
3. 还有哪些已知问题。
4. 下一步用户应该测试什么。
5. 如果需要服务器、域名、API Key、数据库等，必须提前说明。

任何时候，不允许在没有完成第二、第三阶段之前告诉用户“开发完成”。
