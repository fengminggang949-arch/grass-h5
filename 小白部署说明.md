# 小白部署说明（Vercel）

## 1. 准备 GitHub

1. 打开 [GitHub](https://github.com)，点击右上角 **Sign up** 注册，已有账号则点击 **Sign in**。
2. 新建一个仓库，建议名称为 `recommendation-assistant`。
3. 将当前项目上传到这个仓库。如果不熟悉上传，可让开发人员协助一次。不要上传 `.env` 文件。

## 2. 在 Vercel 部署

1. 打开 [Vercel](https://vercel.com)。
2. 点击 **Sign Up**，选择 **Continue with GitHub** 登录。
3. 登录后点击 **Add New…** → **Project**。
4. 在列表中找到 `recommendation-assistant`，点击 **Import**。
5. 在 **Environment Variables** 中添加：
   - `AI_MODE`：先填 `mock`
   - `DATABASE_URL`：正式上线请填 PostgreSQL/Supabase 提供的连接地址，不要用本地 SQLite
   - `ADMIN_PASSWORD`：填一个只有管理员知道的强密码
   - `ADMIN_SESSION_SECRET`：填一串至少32位的随机字符
   - 要使用真实 AI 时，再添加 `AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`，并把 `AI_MODE` 改为 `real`
6. 点击 **Deploy**，等待界面显示 **Congratulations**。
7. 部署成功后，页面会显示一个以 `.vercel.app` 结尾的网址，点击即可访问。

## 3. 生成顾客二维码

1. 打开“你的网址 + `/admin`”。
2. 输入上面设置的管理员密码。
3. 打开“二维码管理”，选择渠道和预选项目。
4. 点击“下载 PNG”，把图片放到前台、收银台或桌牌上。
5. 用微信真机扫码，确认显示正确门店和项目后再给顾客使用。

## 4. 更新新版本

将修改后的文件重新上传到同一个 GitHub 仓库，Vercel 会自动开始新一次部署。部署完成后原网址不变，原二维码也可以继续使用。

> 注意：Vercel 的无服务器环境不适合长期保存 SQLite 文件。正式上线时请使用 Vercel Postgres、Supabase 或其他托管 PostgreSQL，并请开发人员完成一次数据库切换。
