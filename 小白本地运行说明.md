# 小白本地运行说明

## 第一次运行

1. 在 Mac 上按下 `Command + 空格`，输入“终端”，然后打开。Windows 可打开 PowerShell。
2. 在终端中输入：

```bash
cd "/Users/fengminggang/Desktop/小红书种草h5"
```

3. 输入：

```bash
npm install
```

4. 第一次再输入：

```bash
npm run db:push
npm run db:seed
```

5. 启动项目：

```bash
npm run dev
```

6. 打开浏览器，输入 `http://localhost:3000`。看到绿色的“星圣贝口腔朝阳店”欢迎页，就代表成功。
7. 停止运行：回到终端，同时按 `Control + C`。

## 以后再次运行

进入项目目录后，只需运行 `npm run dev`。

## 如果报错

1. 先确认电脑已安装 Node.js 20 或更高版本。
2. 关闭终端，重新打开并按上面步骤再试。
3. 如果提示 3000 端口被占用，页面会显示新地址（例如 `http://localhost:3001`），请打开终端显示的地址。
4. 仍无法解决时，复制终端最后的红色提示给开发人员。不要发送 `.env` 中的密钥。
